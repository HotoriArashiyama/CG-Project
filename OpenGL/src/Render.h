#pragma once

/*
* Most functions of Render is replaced by Layout.h and Control.h.
* Class Render is now empty.
*/

#ifndef __RENDER_H__
#define __RENDER_H__

#include <windows.h>
#include <stdio.h>
#include <stdlib.h>
#include <math.h>

class Render
{
public:
	Render();
	~Render();

private:

};

Render::Render()
{
}

Render::~Render()
{
}

#endif